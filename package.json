{
  "name": "thalmor-clockin",
  "version": "2.0.0",
  "description": "Thalmor Discord duty-hours tracking (/clockin, /clockout) synced to the roster Google Sheet via a Cloudflare Worker",
  "scripts": {
    "test": "node --test tests/",
    "register": "node scripts/register-commands.js",
    "setup-sheet": "node scripts/setup-sheet.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
